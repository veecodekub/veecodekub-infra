resources:
  - ../../base

namespace: nestjs-test

images:
  - name: ghcr.io/veecodekub/nestjs-cicd
    newTag: test

patches:
  - target:
      kind: Deployment
      name: nestjs
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 1
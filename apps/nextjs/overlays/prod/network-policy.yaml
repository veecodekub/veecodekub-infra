apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: nextjs-prod
spec:
  podSelector:
    matchLabels:
      app: nextjs
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
          podSelector:
            matchLabels:
              app.kubernetes.io/name: traefik
      ports:
        - protocol: TCP
          port: 3000
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: nestjs-prod
          podSelector:
            matchLabels:
              app: nestjs
      ports:
        - protocol: TCP
          port: 3000
